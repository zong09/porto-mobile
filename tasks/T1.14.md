# T1.14 — Theme tokens   (Owner: Qwen)

## Goal
Build the design-token classes (`AppColors`, `AppType`, `AppSpacing`, `AppRadii`) straight from
`tasks/design-tokens.md` (CONTRACTS §6). Pure Flutter, no codegen, no font-asset wiring.

## Files (ONLY these)
- `lib/src/ui/theme/colors.dart`
- `lib/src/ui/theme/typography.dart`
- `lib/src/ui/theme/spacing.dart`
- `test/theme_test.dart`

## Read only
This brief + `tasks/design-tokens.md`. Imports `package:flutter/material.dart`. No build_runner.

## colors.dart — `class AppColors` (all `static const Color`)
One `static const Color` per row of the design-tokens color table, using the literal form
`Color(0xFF<6hex>)`. Fields (name → hex):
`bg #FAF5EC · bgAlt #FAF6ED · surface #FFFFFF · text #3D3328 · text2 #6B5D49 · muted #8A7D6C ·
muted2 #A89A86 · muted3 #B3A692 · border #E0D5C2 · brand #EC6530 · brandD #C24A1E · brandDd #9A3614 ·
secondary #FFAE6E · secondaryL #FFC79A · soft #FFE3E3 · softH #FFD4D4 · gain #1E9396 · gainBg #DDF3F3 ·
loss #C73B22 · lossBg #FCDFD4 · gold #E6A23C · rose #C76B8E`

Plus the chart/portfolio palette (order matters):
```dart
static const List<Color> palette = [
  Color(0xFFEC6530), Color(0xFFFFAE6E), Color(0xFF3AA9AC),
  Color(0xFFE6A23C), Color(0xFFC76B8E), Color(0xFF5FBEC0),
];
```

## typography.dart — `class AppType`
- `static const String fontFamily = 'Anuphan';`
- `static const TextStyle` roles (all `fontFamily: fontFamily`):
  - `display` — fontSize 30, FontWeight.w700, color `AppColors.text`
  - `heading` — 20, w700, `AppColors.text`
  - `title` — 16, w600, `AppColors.text`
  - `body` — 14, w400, `AppColors.text`
  - `label` — 12.5, w600, `AppColors.muted`
  - `caption` — 12, w400, `AppColors.muted`
  - `numTabular` — 14, w500, `AppColors.text`, `fontFeatures: [FontFeature.tabularFigures()]`
- Import `AppColors` from `colors.dart`. (`FontFeature`/`FontWeight`/`TextStyle` come from material.)

## spacing.dart
```dart
class AppSpacing {   // static const double
  static const double xs = 6, sm = 10, md = 14, lg = 18, xl = 22, xxl = 26, page = 28;
}
class AppRadii {     // static const double
  static const double input = 12, card = 16, modal = 24, pill = 999;
}
```

## Test (`test/theme_test.dart`)
- `AppColors.brand == const Color(0xFFEC6530)`
- `AppColors.palette.length == 6`
- `AppColors.gain == const Color(0xFF1E9396)`
- `AppSpacing.page == 28` and `AppRadii.modal == 24`
- `AppType.fontFamily == 'Anuphan'` and `AppType.display.fontWeight == FontWeight.w700`
- Pump a `MaterialApp(theme: ThemeData(scaffoldBackgroundColor: AppColors.bg, fontFamily: AppType.fontFamily))`
  with a `Scaffold` body, and expect no exceptions (`await tester.pumpWidget(...)`; `expect(find.byType(Scaffold), findsOneWidget)`).

## Done-when
`flutter analyze lib/src/ui/theme/` clean, `flutter test test/theme_test.dart` passes.
